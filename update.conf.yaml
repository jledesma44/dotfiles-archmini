- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.zprofile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.local/bin:
    ~/.config/bat:
    ~/.config/gtk-3.0:
    ~/.config/gtk-4.0:
    ~/.config/hypr:
    ~/.config/kitty:
    ~/.config/kmonad:
    ~/.config/wlogout:
    ~/.config/matugen:
    ~/.config/nvim:
    ~/.config/nwg-look:
    ~/.config/opencode:
    ~/.config/pulse:
    ~/.config/rofi:
    ~/.config/swaylock:
    ~/.config/waybar:
    ~/Pictures:

- shell:
    - command: ./1.Scripts/packages_install_update.zsh
      stdout: true
      stderr: true
      stdin: true
